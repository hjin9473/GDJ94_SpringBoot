<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.winter.app.board.notice.NoticeDAO">
    	<select id="detail" resultType="NoticeDTO" resultMap="detailResult" parameterType="NoticeDTO">
    		SELECT * 
    		FROM notice N
    				LEFT JOIN
    				notice_files NF
    				ON NF.board_num = N.board_num
    		WHERE N.board_num = #{boardNum}
    	</select>
    	
    	<resultMap type="NoticeDTO" id="detailResult">
    		<id column="board_num" property="boardNum"/>
    		<result column="board_title" property="boardTitle"/>
    		<result column="board_writer" property="boardWriter"/>
    		<result column="board_contents" property="boardContents"/>
    		<result column="board_date" property="boardDate"/>
    		<result column="board_hit" property="boardHit"/>
    		<collection property="fileDTOs" javaType="java.util.List" ofType="BoardFileDTO">
    			<id column="file_num" property="fileNum"/>
    			<result column="file_name" property="fileName"/>
    			<result column="file_origin" property="fileOrigin"/>
    		</collection>
    	</resultMap>
    	
    	<select id="count" resultType="Long" parameterType="Pager">
			    SELECT COUNT(board_num) FROM notice
			    <if test="kind != null and search != null">
			    WHERE
			        <if test="kind == 'k1'">
			            board_title LIKE concat('%',#{search},'%')
			        </if>
			        <if test="kind == 'k2'">
			            board_contents LIKE concat('%',#{search},'%')
			        </if>
			        <if test="kind == 'k3'">
			            board_writer LIKE concat('%',#{search},'%')
			        </if>
			    </if>
			</select>
    	  
       <select id="list" resultType="NoticeDTO" parameterType="Pager">
          SELECT * FROM notice 
          <if test="kind == 'k1'">
          WHERE 
             board_title LIKE concat('%',#{search},'%')
          </if>
          <if test="kind == 'k2'">
          WHERE 
             board_contents LIKE concat('%',#{search},'%')
          </if>
          <if test="kind == 'k3'">
          WHERE 
             board_writer LIKE concat('%',#{search},'%')
          </if>
          
          ORDER BY board_num DESC
          LIMIT #{startNum}, #{perPage}
       </select>

    	<insert id="add" parameterType="NoticeDTO" useGeneratedKeys="true" keyProperty="boardNum">
    		INSERT INTO notice (board_title, board_writer, board_contents, board_date, board_hit)
    		VALUES (#{boardTitle}, #{boardWriter}, #{boardContents}, now(), 0)
    	</insert>
    	
    	<delete id="delete" parameterType="NoticeDTO">
    		DELETE FROM notice WHERE board_num=#{boardNum} 
    	</delete>
    	
    	<update id="update" parameterType="NoticeDTO">
    		UPDATE notice SET board_title=#{boardTitle}, board_contents=#{boardContents} 
    		WHERE board_num=#{boardNum}
    	</update>
    
    	<!-- fileAdd -->
    	<insert id="fileAdd" parameterType="BoardFileDTO">
    		INSERT INTO notice_files (file_name, file_origin, board_num) 
    		VALUES (#{fileName}, #{fileOrigin}, #{boardNum});
    	</insert>
    	
    	<select id="getFileList" resultType="BoardFileDTO" parameterType="BoardFileDTO">
        SELECT *
        FROM notice_files
        WHERE board_num = #{boardNum}
    </select>
    	<!-- fileDetail -->
    	<select id="fileDetail" parameterType="BoardFileDTO" resultType="BoardFileDTO">
    		SELECT * FROM notice_files WHERE file_num=#{fileNum}
    	</select>
    	
    	
    	
    </mapper>